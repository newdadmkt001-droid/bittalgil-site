# bittalgil-site

빚탈길 **배포 산출물**. 손으로 고치지 말 것.

소스와 빌드 스크립트는 `newdadmkt001-droid/bittalgil-web` 에 있다.
거기서 `node build-deploy.js` 로 만든 결과를 이 레포로 밀어 넣는다.

`.nojekyll` 은 GitHub Pages 의 Jekyll 처리를 끄기 위한 것이다
(밑줄로 시작하는 파일이 사라지는 것을 막는다).
